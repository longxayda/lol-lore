this.fname = "Huynhsdads";
this.lname = "Lyan";

const info = {
  fname: "Minhsda",
  lname: "duam",
  getFullName() {
    return `${this.fname} ${this.lname}`;
  },
};

console.log(info.getFullName());

console.log(getInfo());
